# Package
version     = "0.0.1"
author      = "Federico Omoto"
description = "Wrapper around the GMP bindings for the Nim language."
license     = "MIT"
srcDir      = "src"

# Deps
requires "nim >= 1.6.14"
requires "https://github.com/chaemon/nim-gmp >= 0.0.1"
